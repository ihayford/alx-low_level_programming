#include "main.h"
/**
 * _abs  - calculate the absolute value of any integer
 * @t: integer in question
 * Return: Absolute value
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}

